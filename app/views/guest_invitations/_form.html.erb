<%= semantic_form_for(@guest_invitation) do |f| %>
  <%= f.inputs do %>
    <%= f.input :party, :as => :select, :collection => @current_user_parties %>
  	<%= f.input :guest, :as => :select, :collection => @current_user_guests %>
    <%= f.input :expected_attendees %>
    <%= f.input :actual_attendees %>
    <% end %>
  <%= f.buttons %>  
<% end %>
